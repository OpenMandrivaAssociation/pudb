--- a/pyproject.toml
+++ b/pyproject.toml
@@ -1,4 +1,7 @@
 [build-system]
+# OpenMandriva packaging: rely on system-provided hatchling (BuildRequires).
+# Do not bootstrap or download setuptools at build time (same intent as the
+# old distribute_setup / use_setuptools() disable in setup.py).
 requires = ["hatchling"]
 build-backend = "hatchling.build"
 
